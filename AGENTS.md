# 開発ルール

- ファイルを変更した場合は、作業内容を確認したうえで、原則としてコミットし、設定済みのGitHubリモートへプッシュする。
- GitHub Pagesで公開しているため、プッシュ後は必ずGitHub Pagesのデプロイ（公開更新）を作成し、完了・成功まで確認する。自動デプロイが起動しない場合は手動でデプロイを作成する。
- Pagesデプロイは `.github/workflows/pages.yml` の直列化されたワークフローで行い、同時実行によるデプロイ競合を起こさない。デプロイ失敗時は原因を解消してから再実行し、成功を確認する。
- Cloudflareは無料プランで運用する。有料プランへの変更、支払い方法の登録、課金対象サービスの追加はユーザーの明示承認なしに行わない。無料枠に達したら共有を停止・エラー表示し、自動で有料化しない。
- リリース番号を更新する場合は、設定画面の表示、`package.json`、ルートの`sw.js`を同じ番号にそろえる。
- 更新後は設定画面に現在のバージョンが表示されることを必ず確認し、表示番号・`package.json`・ルートの`sw.js`を一致させる。
- コミットやプッシュができない場合は、理由を作業結果に明記する。
- `CODEX_HANDOFF.md` は作業開始時に読み、作業終了時または重要な実装・設計変更時に現在状態へ更新する。
- `CODEX_HANDOFF.md` を更新した場合は、内容を確認したうえで、その作業中に直ちにコミット・プッシュする。次の作業へ持ち越さない。
