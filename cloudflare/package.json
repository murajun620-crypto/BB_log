{
  "name": "courtside-share-service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply courtside-share --local",
    "db:remote": "wrangler d1 migrations apply courtside-share --remote"
  },
  "devDependencies": {
    "wrangler": "4.129.1"
  }
}
