# Project Status

バスケットボールの試合記録・共有を行うオフライン対応PWA。正規リポジトリは `Documents/Github/BB_log`、公開先は `https://murajun620-crypto.github.io/BB_log/`。現在のアプリ版は3.0.1。標準記録、Advanced（Bリーグ式12ゾーン）、iPad横向き中心のPro記録モードを実装済み。作戦ボードは実装を保持したまま、試合画面から一時的に外している。

# Recent Changes

- 3.0.0をv3の区切りとして、記録データ形式・既存イベント処理・旧共有リンク互換を維持したままUI/UXを整理した。LIVE画面の入力手順を短くし、直近表示を2件へ整理、UNDOに対象イベント名を表示し、保存済み表示は必要時だけ出す。設定画面ではAdvancedモードとスリープ防止を初期HTMLへ移し、重複していた接続状態表示とiPhoneインストール手順の常時表示を整理した。共有リンク管理はReader起動・URLコピー・端末取り込みを主要操作として2列にまとめ、再共有・確認・削除を「その他の操作」へ収納し、取り込み済み共有は再取り込み不可と表示する。
- 本体とReaderでBOX SCOREのスタッツキー、ラベル、シューティング平均表記を共通化。Readerの共有レポート末尾表示も短くし、共有リンク受け取り画面の文言を用途に合わせて整理した。UIバージョンは `js/version.js` を単一ソースとし、設定画面、`package.json`、本体／Reader Service Workerを3.0.0へ統一。`npm run check:version`を追加し、Pagesワークフローでバージョン整合と`npm test`（44件）をデプロイ前に自動検証する。
- Pages Run 71でワークフローのNode 20が`tests/cloud-share.test.mjs`の`node:sqlite`を読み込めず失敗したため、Pagesワークフローの実行Nodeを24へ更新。ローカルで`npm run check:version`と`npm test`（44件）を再確認してから再プッシュする。
- v3のPro LIVE操作を補強。選択中のプレーはヘッダーの「キャンセル」または同じプレーボタンの再タップで未選択へ戻せるようにし、交代でOUT選手を選んだ後は他のコート上選手をグレーアウト／無効化、ベンチ選手を強調してIN選択対象にした。味方・相手の交代に共通適用し、OUT選択中でもヘッダーから操作を取り消せる。`npm test`は45件すべて成功。
- 3.0.0の「共有リンクを作成」に任意のタイトルと備考欄を追加。複数試合共有では既存の大会名欄を維持したまま備考欄を追加し、タイトル・備考を共有レポートへ保存する。タイトルは共有リンク管理一覧・作成後の再共有・Readerへ反映し、管理画面のデータ確認にも備考を表示する。旧共有レポートは空のメタデータとして互換読み込みできる。Worker側でもタイトル80文字・備考500文字を検証し、保存時に許可リストへ限定する。
- 3.0.0のスタッツ表示にEFFを追加。EFFは保存データへ新しい項目を追加せず、`PTS + REB + AST + STL + BLK - (FGA - FGM) - (FTA - FTM) - TO` で表示時に算出する。メインアプリとReaderのBOX SCORE、選手詳細、複数試合の合計／平均、Proの相手選手スタッツ、CSVへ共通適用し、旧バックアップ・共有データとの互換性を維持した。`npm test`は48件成功。
- 3.0.1で本体アプリとReaderの座標付きショットチャートを、フルコートから「ゴールが上」のハーフコート○×表示へ統一した。各シュートの攻撃方向を表示時に正規化するため、前半・後半で攻める方向が変わっても同じコートへ合算される。チャート上の位置変更も同じ座標変換を使う。Service Worker、設定画面、`package.json`を3.0.1へ揃え、「最新版を確認」で新しいシェルを検出できるようにした。`npm test`は49件成功。
- 2.2.38で「共有したリンクを管理」に、Readerを新しいタブで開く導線、共有リンクURLだけをコピーするボタン、この端末へ取り込むボタンを追加。管理キーで取得した共有スナップショットを通常の履歴／BOX SCOREで閲覧できる完了試合へ変換し、複数試合の共有は各試合へ分解、得点・ピリオド得点・選手スタッツ・シュート位置を再現する。元のイベントログは共有されないため、取り込み時にその旨を表示し、同じ共有IDの二重取り込みは拒否する。既存端末に同一チーム・選手構成があればチームを再利用し、取り込み保存はIndexedDBの1トランザクションで行う。設定画面・Service Worker・Reader Service Worker・package metadataのバージョンを2.2.38へ更新した。実装コミット `3473a4f` はPages Run 69で成功し、公開設定画面に2.2.38を表示して更新適用を確認した。
- 2.2.8でPro LIVEの選手PTS／F数値を7セグ風から通常フォントへ戻した。表示サイズは維持し、背番号を拡大してテーマのアクセント色で表示する。
- 2.2.9でProのシュートエリア分類をコートの奥行きと実際の3Pコーナー区間で判定し、3Pウイング／コーナー、2Pウイング／コーナーの境界誤認識を修正。Proチャートの共通CSSを本体とReaderで共有し、Readerの独自コート幅差を解消した。ReaderのチームチャートはDOM後挿入ではなく本体と同じレポート描画経路に統合し、座標付き共有データで○×／エリア切替を確実に表示する。
- 2.2.10で本体アプリの「共有」シートに「リンクをコピー」を追加。圧縮したReader共有リンクをクリップボードへ直接コピーし、サーバー保存なしでLINE等へ貼り付けられるようにした。既存の共有APIと同じクリップボード／textareaフォールバックを利用し、コピー成功・失敗とリンク長超過をトーストで通知する。ブラウザE2EにはコピーURLの回帰確認を追加した。
- 2.2.11でProの2Pコーナー判定範囲をショットマップの2Pコーナー／ウイング境界に合わせ、2Pウイングのタップがコーナーへ誤分類される問題を修正。2P／3Pの境界ケースを単体テストへ追加した。
- 2.2.12で3Pラインを元の位置に戻し、フルコートでは上下のサイドライン、iPhoneハーフコートでは左右のサイドラインだけを外側へ広げた。シュート判定の3P境界は変更せず、コート面、バックコートの選択不可領域、Proチャートの外形も表示に合わせて更新した。
- 2.2.13でProの相手選手入力を背番号だけに変更し、名前を要求せずに保存できるようにした。既存の名前付き相手データは読み込めるが、Pro選手欄・選択中表示・履歴・BOX SCOREでは背番号だけを表示する。
- 2.2.14でProの相手選手番号をローラー式の選択UIに変更し、相手チームを先頭5人のコート上選手とベンチ選手に分けて表示。相手交代をOUT→INのSUB履歴として記録し、UNDOと履歴表示に対応した。既存試合の番号変更では選手IDと初期先発を維持し、コート上が5人でない状態では相手交代を開始できないようにした。2P／3Pのコーナー判定・ショットマップ領域をベースライン側へ縮小し、物理的な3Pラインは動かしていない。
- 2.2.15で相手交代の補強変更をService Workerの新しいキャッシュ番号へ反映し、既存の2.2.14キャッシュが残る端末でも交代開始条件を確実に更新できるようにした。
- 2.2.16で本体／ReaderのService WorkerがアセットをHTTPキャッシュから強制再取得して新しいシェルへ格納するようにした。Pagesデプロイ後も古いJavaScriptが新キャッシュへ混入してバージョン表示が戻る問題を防ぐ。
- 2.2.17でProコートのサイドラインをさらに外側へ広げ、3P円弧を拡大して直線部を短縮した。3Pコーナー判定を短縮後の直線部に合わせ、2P／3Pともコーナー範囲をベースライン側へ縮小した。相手選手は半角数字の背番号＋任意名のテキスト入力に戻し、味方・相手とも登録人数の上限を撤廃。Pro LIVEでは両チームの選手一覧末尾から試合中に選手を追加でき、プレー選択→選手→コートの順に次の操作対象だけを強調して他をグレーアウトする。攻撃方向はコート中央に大きく表示する。
- 2.2.18でPro LIVEのグレーアウトを操作可能範囲と分離。プレー未選択時も自チーム／相手チームの交代ボタンを有効にし、プレー選択後もプレー操作パネルで成否やプレー種別を変更できるようにした。選手・コートなど次に選ぶ対象以外の抑制表示は維持し、設定画面・Service Worker・Reader・package metadataのバージョンを2.2.18へ更新する。
- 2.2.19でProの相手選手入力を背番号欄と名前欄の2つのテキストボックスに分離。背番号は1人1行で半角数字1〜3桁、名前は同じ行で任意入力とし、試合作成時と試合中設定で同じUIを使用する。旧「背番号 名前」形式の下書きは分離表示へ互換変換する。
- 2.2.20でPro LIVEのプレー未選択時はコートをグレーアウトせず通常表示に変更。ただし誤タップ防止のためコート操作は無効にし、プレー選択後に選手を待つ状態ではコートをグレーアウトする。
- 2.2.21でPro LIVEのシュート地点確定後、タップ地点にシュートエリア名と強調リングを一時表示し、約3.2秒後にフェードアウトするようにした。2P／3Pのコーナー奥行きをエンドラインからノーチャージ／レーン基準の縦線付近まで210pxへ広げ、左右対称に判定する。
- 2.2.22でProの相手選手入力を、チーム作成と同じ1人1行の行エディタへ統一。背番号と任意の名前を選手ごとに別ボックスで入力でき、試合作成・試合中設定の両方で行の追加・削除に対応した。既存の背番号・名前の複数行形式と旧「背番号 名前」形式は互換変換して表示する。実装コミット `ea04ec4` はPages Run 40で成功し、公開設定画面に2.2.22を表示して確認した。
- 2.2.23でiPhone縦向きのProハーフコートから攻撃方向ボタンを非表示にし、相手チームの個人2P／3Pシュート位置入力を出さないようにした。フリースロー入力、選手選択、交代は維持し、旧選択状態や古いUIからのコートタップも拒否する。実装コミット `1054a01` はPages Run 42で成功し、公開設定画面に2.2.23を表示して確認した。
- 2.2.24でPro LIVEの保存済みシュート○／×をタップすると、選手・シュートエリア・結果・種別をシートで確認できるようにした。座標付きショットチャートの○／×にも同じ詳細導線を追加し、本体のBOX SCORE／選手詳細と共有Readerで共通の表示・キーボード操作に対応した。iPhone縦向きの相手個人入力は位置を求めず、2P／3P／FTの成否ボタンと相手選手タップで得点者・得点を記録できるように戻した。実装コミット `996ad46` はPages Run 44で成功し、公開設定画面に2.2.24を表示して確認した。
- 2.2.25で◯／×タップの詳細表示を修正。共有Readerの座標付きショットはエリアIDから2P／3Pを補完し、詳細シートの「選手・シュートエリア・シュート種別・結果」を本体Pro／BOX SCORE／Readerで揃えた。SVGマーカーには透明なタップ領域を追加し、文字の外側をタップしても詳細を開けるようにした。実装コミット `bb7c19d` はPages Run 46で成功し、公開設定画面で2.2.25を表示して更新適用を確認した。
- 2.2.26で保存済みシュートの◯／×詳細をモーダル／シートから廃止し、押下中だけシュートマーカー付近に小さなインライン枠を表示するようにした。枠には結果・シュート種別・選手・エリアを表示し、pointerup／pointercancel／画面離脱またはキー解放で即時消去する。本体Pro LIVE・BOX SCORE／選手詳細のチャート・共有Readerへ共通適用した。実装コミット `7d83d63` はPages Run 48で成功し、公開設定画面に2.2.26を表示して確認した。
- 2.2.27で◯／×の詳細インライン枠の文字を16pxへ拡大し、3行の行間と枠高さを調整した。SVGコート・マーカー・詳細枠にuser-select／長押しメニュー／ドラッグ禁止を適用し、本体とReaderでselectstart／dragstartも抑止して、◯／×やコート全体が文字選択される問題を防いだ。実装コミット `dd9b97c` はPages Run 50で成功した。
- 2.2.28で◯／×を押している間だけ表示する詳細枠から結果・シュート種別の一行目を削除し、選手とシュートエリアの2行に整理した。文字量に応じて枠幅を144〜184pxへ縮め、枠高も54pxへ調整した。本体Pro／BOX SCORE／共有Readerに共通適用した。実装コミット `b33b5a7` はPages Run 52で成功した。
- 2.2.29で保存済みシュートの◯／×は通常タップで詳細インライン枠を表示し、アプリ画面の別箇所タップで消える仕様へ変更。長押し／3Dタッチ（右クリック・強押しイベントを含む）では位置変更・選手変更・削除の編集シートを開く。Pro LIVEの新規シュートはタップ後のドラッグ終点を記録し、保存済みシュートの位置編集もドラッグ終点へ移動できる。本体のLIVE／BOX SCORE／選手詳細に適用し、Readerはタップ詳細のみの読み取り専用とした。実装コミット `6a77b89` はPages Run 54で成功し、公開設定画面に2.2.29を表示して確認した。
- 2.2.31でProコートの2Pコーナー／ウイング境界を左 x=120・右 x=820へ追加し、3Pの既存境界（左 x=210・右 x=730）とともに白い基準線で表示。3Pラインの白い円弧・新規シュートの2P/3P判定・エリア表示・Advanced picker・Readerを同じ座標定義へ統一した。保存済み記録は eventType を優先してエリアを再表示するため、履歴・BOX SCORE・Readerで2P/3Pが入れ替わらない。実装コミット `8aedd1d` はPages Run 56で成功し、公開設定画面に2.2.31を表示して確認した。
- 2.2.32でProの3Pコーナー／ウイング境界も2Pと同じ左 x=120・右 x=820へ統一した。LIVE・ハーフコートのコート描画からエリア判定用の白い補助線を撤去し、実際のコートラインだけを残す。新規シュートは成功○／失敗×のプレビューがドラッグ中に指先へ追従し、保存済みシュートの位置編集は既存の○／×自体をリアルタイムに追従させる。キャンセル・バックコート・保存失敗時は元位置を復元する。実装コミット `e87d8b0` はPages Run 58で成功し、公開設定画面に2.2.32を表示して確認した。
- 2.2.33でPro LIVEのプレー未選択時は選手一覧を通常の濃さで表示し、選手選択後のコート選択待ち状態だけをグレーアウトするようにした。プレー未選択時の選手操作は無効のまま維持する。実装コミット `3495e02` はPages Run 60で成功し、公開設定画面に2.2.33を表示して確認した。
- 2.2.34でシュートエリアの左右を攻撃方向から見た選手基準へ統一。Proの新規記録・位置変更・LIVEのフィードバック・履歴・BOX SCORE／選手詳細・本体のエリア／ポイント表示・共有Readerで同じ左右を使う。左攻撃時の左右反転を座標判定に反映し、共有レポートと圧縮リンクには表示方向を互換性を保って追加した。方向情報のない旧データは従来の固定座標判定へフォールバックする。
- 2.2.35でPro LIVEの攻撃方向ボタンをセンターサークル上からフルコート上端の中央、上側サイドライン付近へ移動した。iPhone縦向きのハーフコートでは従来どおり攻撃方向を表示しない。設定画面・Service Worker・Reader・package metadataのバージョンを2.2.35へ更新した。
- 2.2.36でPro LIVEの味方・相手選手一覧を、コート上の選手を先頭にしたまま、コート上・ベンチそれぞれ背番号の昇順で表示するようにした。交代後も再描画時に同じ番号順へ並べ替え、登録配列の順番に依存しないようにした。設定画面・Service Worker・Reader・package metadataのバージョンを2.2.36へ更新した。
- 2.2.37でPro LIVEの新規シュート位置入力中は、保存済みの○／×をグレーアウトして入力対象から外すようにした。既存マーカー上へイベントが届いた場合も新規位置入力を優先し、シュート記録完了後は選択解除と再描画で通常表示へ戻す。設定画面・Service Worker・Reader・package metadataのバージョンを2.2.37へ更新した。
- 2.2.7でPro LIVEの選手欄を選手名・PTS・Fの構成に整理し、PTS／Fラベルとリアルタイム数値を大きく表示。数値はクロックと同じ7セグ風表示を維持し、PTSを緑、Fを赤で表示する。狭い横向き画面では縮小して収める。
- 2.2.6でPro LIVEのスコアを赤、ゲームクロックをオレンジ、それ以外のスコアボード文字を白に変更した。選手欄のリアルタイムPTS／ファウルもクロックと同じ7セグ風表示にし、PTSを緑、Fを赤で表示する。
- 2.2.5で作戦ボードの起動導線を標準／Proの試合画面から外し、ライブ画面に残っていたボタン用の空き行も除去した。作戦ボード本体は再開判断に備えて休止状態で保持し、試合記録・履歴・共有データには影響しない。
- 2.2.4でPro LIVEのiPhone表示を縦向きだけ上向きゴールのハーフコートに限定し、横向きはiPadと同じフルコートに戻した。ハーフコートの3P・フリースロー・ゴール下の白線円弧も、ゴールが上側になる向きへ修正した。
- 作戦ボードの起動ボタンをUNDO行の直下に置き、ボードは横画面で全画面表示にした。味方・相手・ボールは左側、ライン・矢印・消しゴムは右側の空き領域に配置し、中央コートを画面高さいっぱいまで拡大した。ユニフォーム／ボールはドラッグで移動でき、配置・移動・描画・削除・全消去をUNDOで順に戻せる。右上の×はシート上で直接イベントを受け、確実に閉じる。
- Pro LIVE／Proショットチャートのコートを、変形した半コートの転用から専用フルコートへ置換。3Pは円弧とコーナー直線、両端のレーン・ゴール下・フリースロー円を同一寸法で左右対称にし、横長表示でもコートの実寸比を維持する。Proの2P／3P自動判定も同じコート寸法に合わせた。細いゾーン境界はAdvancedのコーナー／ウイング／トップ構成に合わせ、3P線・レーン・中盤へ接続する。
- Proの自チーム攻撃方向を表示・タップ切替可能にした。方向は前半の基準を保存し、4QではQ3開始時に自動反転する（延長はQ4の方向を継続）。
- Proでフィールドゴールのシュート位置を選択するとき、攻撃方向と現在のピリオドからフロントコートを判定し、反対側のバックコートをグレーアウトして選択不可にした。センターライン上もバックコート扱いとし、相手選手のシュートでは選択可能なコートを自動的に反転する。
- Proのシュートエリア分類を表示中のコート線に合わせ、3Pはコーナーの横幅と奥行き、2Pはレーン側の奥行きも含めて判定。座標を持つ既存シュートは履歴・チャート・共有時にも座標を優先して再分類する。
- Proの点指定・シュートチャートから細いゾーン境界線を除去。座標付きシュートの表示は、○×の点表示とエリア集計表示を本体・Readerの両方で切り替え可能にした。
- 試合中の本体画面に作戦ボードを追加。味方・相手を別色のユニフォームアイコンで各5人まで配置し、番号はチームごとに1〜5を割り当てる。バスケットボールのアイコンは1個だけ配置し、ボード内容は試合データや共有レポートには保存しない。
- プッシュ後にGitHub Pagesデプロイを必ず作成し、完了・成功まで確認する運用を `AGENTS.md` に追加。機能コミット `33ac148` はPages Run 5、引き継ぎ書更新コミット `fe2e0fb` はRun 6、作戦ボード機能コミット `7113b9b` はRun 8、全画面作戦ボードコミット `7e6cd07` はRun 10、iPhoneハーフコート修正コミット `c73581c` はRun 13で成功し、公開URLへの反映を確認した。
- Pagesの連続プッシュ競合を解消するため、`github-pages` concurrencyでデプロイを直列化する `.github/workflows/pages.yml` を追加。Pagesの公開元をGitHub Actionsへ切り替え、Run 3で成功を確認した。
- 古いPagesデプロイがAPI上で `in_progress` のまま残る場合に備え、デプロイ直前にPages APIで停滞デプロイをキャンセルする処理を追加。Run 3で実際に旧デプロイを解放して公開できた。
- 更新後の設定画面に現在のバージョンを表示することを運用ルールへ追加し、表示番号・`package.json`・ルートの`sw.js`の一致を確認する。
- Proのスコア、チーム名、ゲームクロックを黒地・黄色の一体型電光掲示板に統一。ライト／ダークどちらでも同じ見た目にする。ショットクロックは実装しない。
- LIVE中の試合メニューから、標準／Pro、ゲームクロック有無、相手記録（総得点／個人）を変更可能。選手得点・ファウル、交代順、シュート後の選択解除、クロック編集にも対応。
- アプリ本体、Reader、Service Worker、package metadataのバージョンを2.2.37へ更新。

# Current Issues

- Proの相手個人スタッツは本体の試合・BOX SCOREには保存／表示するが、共有レポートでは自チーム中心の共有仕様を維持しており、相手個人表は共有対象外。
- Cloudflare共有リンクの「この端末に取り込む」は、管理キーで取得した集計スナップショットを端末内の完了試合として保存する。元のイベント履歴は共有されていないため、取り込み前の確認画面で集計値を再現したローカル記録であることを案内する。
- 共有リンクのタイトル・備考対応はコードとPagesへ反映済み。ただしCloudflare Worker本番デプロイは、実行環境に`CLOUDFLARE_API_TOKEN`がないためWrangler認証前に停止した。認証済み環境で`cloudflare`ディレクトリから`pnpm run deploy`を再実行する必要がある。ローカルWorkerテストは成功している。
- GitHub Pagesの旧ブランチ連動RunやカスタムRun 1/2は旧デプロイとの競合で失敗したが、PagesをUnpublishしてActions公開へ切り替え、停滞デプロイの自動キャンセルを追加した。その後のRun 3（`ad3655c`）、Run 4（`73cf2d4`）、Run 5（`33ac148`）、Run 6（`fe2e0fb`）、Run 7（`0085988`）、Run 8（`7113b9b`）、Run 10（`7e6cd07`）、Run 13（`c73581c`）、Run 24（`8b228ad`）、Run 25（`89376c2`）、Run 26（`51422b0`）、Run 27（`1d181dd`）、Run 28（`2a12a83`）、Run 30（`4596c05`）、Run 32（`b6e2f9b`）、Run 34（`1abfa93`）、Run 36（`7e7c9f9`）、Run 38（`c751e1c`）、Run 40（`ea04ec4`）、Run 42（`1054a01`）、Run 44（`996ad46`）、Run 46（`bb7c19d`）、Run 47（`c66eaa4`）、Run 48（`7d83d63`）、Run 49（`71afa14`）、Run 50（`dd9b97c`）、Run 51（`45c174c`）、Run 52（`b33b5a7`）、Run 54（`6a77b89`）は成功。過去の公開URL反映を確認済み。最新の公開確認は下記の各バージョン記録を参照。既存ブラウザでは旧Service Workerが残る場合があるため、更新後は設定画面の「最新版を確認」から更新を適用する。
- 2.2.31の実装コミット `8aedd1d` はPages Run 56で成功。公開URLの設定画面で「最新版に更新」を適用し、`COURTSIDE 2.2.31` の表示を確認した。
- 2.2.32の実装コミット `e87d8b0` はPages Run 58で成功。公開URLの設定画面で「最新版に更新」を適用し、`COURTSIDE 2.2.32` の表示を確認した。
- 2.2.33の実装コミット `3495e02` はPages Run 60で成功。公開URLの設定画面で「最新版に更新」を適用し、`COURTSIDE 2.2.33` の表示を確認した。
- 2.2.34の実装コミット `0171ede` はPages Run 62で成功。公開設定画面で旧Service Workerの更新を適用し、`COURTSIDE 2.2.34`表示と「最新版を確認」状態を確認した。
- 2.2.36の実装コミット `3be967d` はPages Run 65で成功。公開設定画面で旧Service Workerの更新を適用し、`COURTSIDE 2.2.36`表示と「最新版を確認」状態を確認した。
- 2.2.37の実装コミット `9fb5d9b` はPages Run 67で成功。公開設定画面で旧Service Workerの更新を適用し、`COURTSIDE 2.2.37`表示と「最新版を確認」状態を確認した。
- 2.2.38の実装コミット `3473a4f` はPages Run 69で成功。公開設定画面で旧Service Workerの更新を適用し、`COURTSIDE 2.2.38`表示と「最新版を確認」状態を確認した。共有リンク管理の実データ操作（URLコピー・Reader起動・端末取り込み）は、管理キーと対象共有を用いた回帰確認が残っている。
- 自動ブラウザ回帰テストは、この環境にPlaywright依存がないため未実行。単体テストとローカルブラウザ操作確認は完了している。
- 今回の共有リンクメタデータ変更では、`npm test`（47件成功）、`npm run check:version`、変更JSの構文チェック、差分チェックを確認した。`npm run test:e2e`はPlaywright未導入のため実行できなかった。
- EFF追加では、`npm test`（48件成功）、`npm run check:version`、変更JSの構文チェック、差分チェックを確認した。EFFの算出式とCSV列も単体テストで確認済み。
- 3.0.1のショットチャート／更新確認変更では、`npm test`（49件成功）、`npm run check:version`、差分チェックを確認した。Pages公開後に設定画面の`COURTSIDE 3.0.1`表示と「最新版を確認」の更新適用を確認する。
- iPhone／iPad実機でのPro縦長ハーフコート／横長フルコートの最終的なタップ感とコート表示は、ユーザー環境で確認が必要。
- 作戦ボードはユーザー判断で一時停止中。再開の指示があるまで、試合画面へ起動ボタンを戻さない。

# Next Tasks

1. iPad／iPhone実機で2.2.38更新を適用し、Pro LIVEで新規シュート位置を選ぶ間だけ保存済み○／×がグレーアウト・非選択となり、記録後に通常表示へ戻ることを確認する。
2. 新規シュートをタップ後にドラッグした終点へ○／×プレビューが追従して保存されること、保存済み○／×の位置変更で実マーカーがリアルタイムに追従し、キャンセル・バックコート操作・保存失敗で元位置へ戻ることを実機で確認する。既存のiPhone縦向きハーフコート、○×／エリア表示切替、選手・相手選手・交代、共有Reader、「リンクをコピー」も回帰確認する。
3. 共有用管理キーで「共有したリンクを管理」を開き、各共有のURLコピー、Reader起動、端末取り込み（単試合・複数試合）と二重取り込み拒否をブラウザで確認する。

# Important Decisions

- 通常記録は端末内保存。共有時は集計データと自チームのシュート情報だけを共有し、元のイベントログや端末IDは出さない。
- 旧共有リンク、旧形式の共有ファイル、旧記録の位置なしイベントを壊さない。FD欠落や旧統計配列は0として補完する。
- Proの正確な位置は0〜1の正規化座標で保存し、表示時にコートへ変換する。エリア集計は同じ座標から算出する。攻撃方向は前半の基準方向を保存し、4Qの後半は表示時に反転する。シュートエリアの左右は攻撃方向から見た選手基準で判定し、共有時は各シュートに方向を保持する。方向なしの旧共有データは固定座標の旧仕様で読める。
- Cloudflareは無料プラン運用。有料化、支払い情報操作、課金対象サービス追加はユーザーの明示承認なしに行わない。
- Cloudflare共有の端末取り込みは、共有スナップショットから選手別スタッツ、シュート位置、ピリオド得点を再構成した完了試合として保存する。元の非公開イベントログは復元しない。既存端末に同じチーム・選手構成がある場合はそのチームを再利用し、共有IDで二重取り込みを防ぐ。
- 変更後は原則コミット・プッシュし、GitHub Pagesの反映を確認する。長期ルールは `AGENTS.md` に置く。

# Environment / Testing Notes

- Windows。Cloudflare Worker関連は `cloudflare/`。共有Workerは `https://courtside-share.murajun620.workers.dev`。
- 単体／Workerテスト：`node --test tests/domain.test.mjs tests/cloud-share.test.mjs`。今回も `node --test tests/*.test.mjs`（43件成功）、変更JSの構文チェックと差分チェックを確認済み。ハーフコートの左右攻撃方向変換、ゴール上向き固定、iPhone UAでは縦向きだけハーフコートを選ぶこと、白線円弧の向きを単体テストで確認済み。2.2.34では左右攻撃方向のゾーン判定、ポイント／エリア表示の一致、共有圧縮リンクの方向保持もテスト済み。2.2.36ではPro LIVEの味方・相手のコート上／ベンチ選手を背番号順に表示し、交代後も番号順を維持することをテスト済み。作戦ボードは2.2.5でUIから一時停止したため、試合画面に入口が表示されないことをローカルで確認する。Pro LIVEのスコア／クロック色、選手PTS／Fの通常フォント・拡大表示用マークアップ、相手選手の半角数字入力・任意名表示、1人1行の番号／名前行エディタと旧形式互換、登録人数上限なし、相手交代の履歴整合性、縮小した2P／3Pコーナー境界、プレー未選択時の交代ボタン、プレー選択後の操作パネル変更とコート／選手操作対象切替、iPhone縦向きの攻撃方向非表示・相手2P／3P位置入力非表示、iPhone相手シュートの位置なし得点記録、保存済み○×をタップで表示し画面外タップで消える選手・エリアのインライン詳細枠、長押し／3Dタッチの位置変更・選手変更・削除、タップ後ドラッグのシュート位置記録、文字サイズ16px、◯／×とコート全体の選択抑止を静的テストで確認済み。
- 自動ブラウザテスト：`node tests/browser.mjs`。実行にはPlaywrightが必要だが、この環境では未導入。
- 2.2.38では `npm test`（43件成功）、変更JS／Service Workerの構文チェック、差分チェック、共有スナップショットから単試合・複数試合を端末内の完了試合へ変換し、得点・ピリオド得点・選手スタッツ・シュート位置を保持するテストを確認済み。Pages Run 69成功後、公開設定画面で2.2.38表示へ更新適用した。
- 手動確認用ローカルサーバーは `node scripts/serve.mjs`。Pro LIVEで実寸比のフルコート、3P直線・円弧、左右レーン、黒黄スコア／クロックを確認済み。
- 2.2.32では `npm test`（39件成功）、変更JSの構文チェック、差分チェック、ローカルのLIVEコートで補助白線がないこと、ローカル／公開設定画面の `COURTSIDE 2.2.32` 表示を確認済み。
- 2.2.33では `npm test`（39件成功）、変更JSの構文チェック、差分チェック、ローカルLIVEでプレー未選択時の選手表示が不透明度1・グレースケールなし、コート選択時だけ不透明度0.28になること、ローカル／公開設定画面の `COURTSIDE 2.2.33` 表示を確認済み。
- `Documents/ChatGPT/BB_log_` は退避フォルダーで開発対象外。管理キーなどの秘密情報はGitへ追加しない。
